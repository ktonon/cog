# Ensure we require the local version and not one we might have installed already
require File.join([File.dirname(__FILE__),'lib','<%= @plugin_name %>/version.rb'])
spec = Gem::Specification.new do |s| 
  s.name = '<%= @plugin_name %>'
  s.version = <%= @plugin_module %>::VERSION
  s.author = '<%= @plugin_author %>'
  s.email = '<%= @plugin_email %>'
  s.homepage = ''
  s.platform = Gem::Platform::RUBY
  s.summary = '<%= @plugin_description %>'
  s.files = %w(LICENSE) + Dir.glob('cog/**/*') + Dir.glob('lib/**/*.rb')
  s.require_paths << 'lib'
  s.has_rdoc = 'yard'
  s.rdoc_options << '--title' << '<%= @plugin_name %>' << '-ri'
  s.add_development_dependency('rake')
  s.add_development_dependency('yard')
end
