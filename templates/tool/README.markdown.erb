= <%= name %> 

<%= description %>

Get it
------

From a terminal

```bash
gem install <%= name %> 
```

or in your Gemfile

```ruby
gem "<%= name %>", "~> 0.0.1"
```

Prepare a project
-----------------

This tool relies on [cog](http://github.com/ktonon/cog), so as part of the
initialization, a `Cogfile` will be created if one does not already exist.